{
  "name": "repro-puppeteer-screenshot-hang",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.mjs",
    "docker": "docker build -t repro . && docker run --rm -p 8181:8181 repro"
  },
  "dependencies": {
    "puppeteer-core": "latest"
  }
}
